# ========================================
# [TRAVELER 프로젝트] 상용 서비스 전용 의존성 명세서
# 파일명: requirements.txt
# Python 버전: Python 3.11+ 기준
# 최종 업데이트: 2026-05-22
# ========================================

# ========================================
# [1] Core — 환경 설정 및 데이터 검증
# ========================================
python-dotenv>=1.0.0
# 역할: 환경변수 관리
# 설명: .env 파일에서 OpenAI, Pinecone, 외부 API Key 등 보안 민감 정보를 안전하게 로드

pydantic>=2.5.0
# 역할: 데이터 스키마 검증 (Guardrails 1단계)
# 설명: 프론트엔드가 보낸 실시간 차량 GPS 좌표와 LLM이 뱉는 JSON 출력 포맷의 무결성을 검증

pydantic-settings>=2.1.0
# 역할: 백엔드 설정 중앙 관리
# 설명: .env 환경변수들을 파이썬 클래스 형태로 묶어 서버 내에서 더 안전하고 체계적으로 관리

# ========================================
# [2] LLM & RAG — AI 추천 코어 파이프라인
# ========================================
openai>=1.14.0
# 역할: OpenAI 공식 클라이언트
# 설명: GPT-4o 모델 호출 및 1536차원 임베딩 변환 수행

langchain>=0.2.0
# 역할: RAG 프레임워크 컨트롤 타워 
# 설명: 유저 질문 -> DB 검색 -> LLM 답변으로 이어지는 전체 흐름을 엮는 엔진. v0.2.0에서 LCEL 안정화 완료

langchain-openai>=0.1.6
# 역할: LangChain - OpenAI 전용 연동 컴포넌트
# 설명: LangChain 파이프라인 안에서 GPT-4o 모델과 임베딩 API를 에러 없이 다이렉트로 제어

langchain-community>=0.2.0
# 역할: 랭체인 외부 생태계 통합 도구
# 설명: SQLite나 Pinecone 같은 외부 DB 도구들을 LangChain에 블록처럼 끼워 맞추기 위한 확장 패키지

# ========================================
# [3] Vector DB — 벡터 데이터베이스 (이수연 담당)
# ========================================
pinecone>=3.0.0
# 역할: Pinecone 벡터 DB 클라이언트 (최신 패키지명 명칭 변경 반영)
# 설명: 분위기/감성 기반 유사 장소 검색을 위해 클라우드 Pinecone DB에 쿼리를 날리는 핵심 라이브러리

# ========================================
# [4] HTTP 클라이언트 — 외부 API 통신
# ========================================
httpx>=0.27.0
# 역할: 비동기 HTTP 요청 처리 (백엔드 필수)
# 설명: 기상청 단기예보 및 Tmap 경로 API를 백엔드에서 '동시에' 비동기로 때려 호출 지연을 최소화함

requests>=2.31.0
# 역할: 동기 HTTP 요청 처리
# 설명: TourAPI 관광지 데이터 대량 수집 등 실시간성이 필요 없는 동기 작업에 활용

# ========================================
# [5] Web Framework — FastAPI 백엔드 서버 (이수연 담당)
# ========================================
fastapi>=0.104.1
# 역할: 비동기 고성능 웹 프레임워크
# 설명: 운전자의 실시간 요청을 받아서 RAG를 돌린 뒤 프론트(React)에 던져주는 메인 허브 API 서버

uvicorn>=0.24.0
# 역할: ASGI 웹 서버 구동 엔진
# 설명: 수연이가 만든 FastAPI 애플리케이션을 로컬 및 AWS 클라우드 환경에서 안정적으로 가동해 주는 엔진

# ========================================
# [6] Database — 관계형 데이터베이스
# ========================================
sqlalchemy>=2.0.23
# 역할: SQLite ORM 라이브러리
# 설명: SQL을 직접 쓰지 않고 파이썬 코드로 로컬 SQLite DB의 유저 페르소나 및 대화 로그 테이블을 안전하게 제어

# ========================================
# [7] 데이터 처리 — 전처리 및 수치 연산
# ========================================
pandas>=2.1.3
numpy>=1.26.0
# 역할: 데이터 구조화 및 고속 벡터 연산
# 설명: 크롤링해 온 관광지 원본 데이터를 데이터프레임으로 핸들링하고, 벡터 차원 수치를 계산할 때 활용

# ========================================
# [8] Monitoring — 모니터링 및 로그 (가영 툴 복구 - 백엔드 연동)
# ========================================
python-logging-loki==0.3.2
# 역할: 실시간 시스템 로그 수집
# 설명: FastAPI 서버가 돌아가면서 발생하는 에러나 유저 요청 로그를 중앙 서버로 쏴주는 백엔드 모니터링 필수 패키지